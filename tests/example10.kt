fun main() {
    var a : Int = 4
    var b : Int = 6
    var eq : Bool = a == b
    var gt : Bool = a > b
    var gtEq : Bool = a >= b
    var lt : Bool = a < b 
    var ltEq : Bool = a <= b
    var diff : Bool = a != b
    diff = b >= a
    print("nothing and anything")
}